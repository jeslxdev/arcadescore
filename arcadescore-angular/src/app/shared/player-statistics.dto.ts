export interface PlayerStatisticsDto {
  player: string;
  totalGames: number;
  averageScore: number;
  highestScore: number;
  lowestScore: number;
  recordBeatenTimes: number;
  playingTime: string;
}
